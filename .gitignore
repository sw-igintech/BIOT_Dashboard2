# Local Claude workspace — credentials, debug scripts, field notes
# NEVER commit anything under claude/
claude/

# Python
__pycache__/
*.pyc

# Reference project symlink (local only — not part of the dashboard codebase)
GitLabTandemG

# Supabase CLI temp files
supabase/.temp/

# Local dev env
.env.local

# OS
.DS_Store

# Deno Deploy CLI writes this config artifact on deploy — do not commit
deno.jsonc
deno/deno.jsonc

# Node deps (e.g. local Playwright for preview E2E) — never commit
node_modules/
