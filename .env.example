# BIOT credentials — set these as Supabase secrets (server-side only, never in git)
# Run:  supabase secrets set BIOT_BASE_URL=... BIOT_USERNAME=... BIOT_PASSWORD=...

BIOT_BASE_URL=https://api.dev.igin.biot-med.com
BIOT_USERNAME=your-biot-username
BIOT_PASSWORD=your-biot-password
